- name: Install k3s agent and join cluster
  shell: >
    curl -sfL https://get.k3s.io |
    K3S_URL=https://{{ hostvars['k3s-server']['private_ip'] }}:6443
    K3S_TOKEN={{ hostvars['k3s-server']['k3s_token'] }}
    sh -
  args:
    creates: /usr/local/bin/k3s-agent-uninstall.sh
